# Hunters Ridge Community website

A responsive neighborhood website with Zelle and check instructions for annual dues.

## Run it

1. Run `npm start`.
2. Open `http://localhost:3000`.

No package installation is required; the server uses Node's built-in tools. The site currently presents Zelle and check as the only payment methods and does not initiate online card payments.
